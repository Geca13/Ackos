const initialState = {
    id: 0,
    firstName: '',
    lastName: '',
    email: '',
    password: '' ,
    isLoggedIn: false
}

export default function authReducer(state= initialState, action) {
    
 return state;
}